kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  annotations:
    argocd.argoproj.io/syncwave: {{ $.Values.syncwaves.role | quote }}
  name: job-patch-operator-role
rules:
  - verbs:
      - get
      - list
      - watch
      - patch
    apiGroups:
      - operators.coreos.com
    resources:
      - installplans
